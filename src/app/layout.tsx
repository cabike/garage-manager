import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Garage Manager - Your Automotive Command Center',
  description: 'Track your cars, tools, parts, projects, and maintenance schedules',
  keywords: 'garage, car management, automotive, maintenance, tools',
  authors: [{ name: 'Nathan Dougan' }],
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}